# tables/ — Reorganized

Tables have been redistributed into the new results structure:

- **`baseline/tables/`** — Publication tables (`table1`–`table4`), experiment summaries (`exp1`–`exp4`), statistical analysis, production results, queue metrics, and other canonical tables.
- **`analysis/tables/`** — CBD comparison tables, legacy comparison tables (`exp2_pivot_rt_with_legacy`).
- **`archive/tables/`** — Legacy tables (old optimization results with cap=5, older notebook-generated CSVs).

This directory retains only `.gitkeep` and this README.
